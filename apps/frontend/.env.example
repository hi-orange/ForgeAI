# Leave empty to use Vite proxy (/api -> http://localhost:8000)
VITE_API_BASE_URL=

# Keep the current UI independent from the removed legacy generator endpoints.
# Change to "api" only after the new BuildRun adapter is implemented.
VITE_PROJECT_DATA_SOURCE=mock
