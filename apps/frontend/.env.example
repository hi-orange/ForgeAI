# Leave empty to use Vite proxy (/api -> http://localhost:8000)
VITE_API_BASE_URL=

# Default: real backend (create project → requirements workbench → BuildRun status).
# Set to "mock" only for the legacy static website demo workbench.
VITE_PROJECT_DATA_SOURCE=api
